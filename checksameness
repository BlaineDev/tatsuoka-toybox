
%% move from java to matlab @July 11
    xlength = 20, ylength = 20;     % the size of the array
    int zlength = 14;               % the number of subjects (FileJ and fname1 or FileK and fname2)
    fname1                          % pass in the patients for the group to check
   
  
  
  
for(zindex = 0 < zlength-1 
  
    
      for yindex = 0 < ylength
     
            for(xindex = 0 < xlength)     
           
               
             BoxOne = fname1(zindex,xindex,yindex);    
             BoxTwo = fname1(zindex+1,xindex,yindex); 
             
             if (fnameOne != BoxTwo)
                
                                                %%break;  
                                                %% fix increment loop for Z and add scale
             
             if (zindex == zlength-2)    //check last 2 elements boolean
                                               %% add print line System.out.println(" all same" );
               
               
